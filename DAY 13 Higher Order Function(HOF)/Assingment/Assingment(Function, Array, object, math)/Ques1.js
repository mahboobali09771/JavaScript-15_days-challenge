/**
Q1. Create an arrow function called square that takes a number as an argument and returns its square. Use the arrow function to calculate the square of a given number and display the resuit.

 */
// solution:-
const square = (num) => num*num
const number = 100
const result = square(number)
console.log(`square of a number is = ${result}`)
